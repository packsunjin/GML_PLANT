# CLAUDE.md — 이 저장소에서 일할 때 먼저 읽을 것

> ## ⚠️ 이 파일을 같이 갱신할 것
> **코드를 고칠 때마다 이 파일도 같이 고쳐서 같은 커밋에 넣는다.** 별도 커밋으로
> 미루지 마라. 아래에 해당하면 반드시 갱신한다.
>
> - 모듈을 추가·삭제하거나 역할이 바뀜 → 2절 구조
> - 상수·기본값·CLI 인자가 바뀜 → 해당 절
> - 부품을 사거나 배선이 바뀜 → 1절 하드웨어
> - **실측 데이터를 처음으로 얻음** → 0절 ①을 고쳐 쓴다 (제일 중요)
> - 틀린 값·틀린 주장을 발견해 고침 → 0절에 항목을 추가한다
> - 보고서 양식·빌드 절차가 바뀜 → 3절
>
> 0절은 지우지 말고 쌓아라. 과거의 실수 목록이 이 파일의 핵심이다.


영천고 Green Mind Lab(GML), 제6회 학생 SW·AI 인재 양성 프로젝트.
방울토마토의 전기신호를 측정해 **정상 / 자극 / 수분부족** 3상태를 분류하는 시스템.

---

## 0. 이것부터 — 지금까지 저지른 큰 실수들

같은 실수를 또 하지 말라고 적어 둔다. 이 절이 이 문서에서 제일 중요하다.

### ① 시뮬레이터 출력을 실측 데이터로 착각했다
`data/raw/{정상,자극,수분부족}.csv` 는 **전부 시뮬레이터 출력이다. 실측이 아니다.**
수집 당시 코드는 하드웨어 감지가 깨져 있어(`b4e9afb` 에서 수정) 항상 시뮬레이션으로
넘어갔고, 측정자 본인도 "애초에 측정을 안 했다"고 확인했다.

**이 프로젝트는 아직 검증된 실측 데이터가 단 한 건도 없다.**
`models/` 안의 정확도·혼동행렬은 전부 시뮬레이터를 학습한 결과이므로 의미가 없다.
보고서에 실으면 안 되고, "정확도가 나왔다"는 식으로 말하면 안 된다.

지금은 `collect()` 가 CSV 옆에 `.meta.json`(mode: hardware/simulation)을 같이 쓰고,
`preprocess.py` 가 가짜가 섞이면 크게 알린다. 출처 파일이 없으면 "실측"이 아니라
**"알 수 없음"** 으로 취급한다.

### ② 없는 결과를 지어냈다
"자극은 분류가 됐다", "실제 결과가 정확히 그러했다" 같은 문장을 근거 없이 보고서에
넣었다(`4442063` 에서 철회). 계산이 결과와 맞아떨어지면 좋겠다는 마음이 근거를
앞질렀다. **측정된 적 없는 것을 측정됐다고 쓰지 마라.**

### ③ 물리량을 섞어 썼다 — 전파 속도 ≠ 파형 지속시간
토마토 AP 전파 속도 5.6 cm/min 을 전극 간격 3 cm 로 나눠 32초 → 0.03 Hz 를 얻고
"AP도 통과대역 밖"이라고 썼다. **그 32초는 신호가 전극 사이를 건너가는 시간이지
전극 하나가 보는 파형의 지속시간이 아니다.**

올바른 값 (참고문헌 [9]):

| 신호 | 지속시간 | 주파수 | AD8232(0.5–40 Hz) |
|---|---|---|---|
| AP 과도 성분 | 0.025–2 초 | 0.5–40 Hz | **통과** |
| 변동전위(VP) | 수 분~수십 분 | ~10⁻³ Hz | 차단 |
| 수분 스트레스 | 수 시간~수 일 | ~10⁻⁴ Hz | 차단(100 dB 이상) |

즉 **수분부족은 원리적으로 불가능함이 증명된 것이고, 자극은 가능하나 미검증**이다.
둘의 성격이 다르다. 뭉뚱그리지 마라.

### ④ 사용자가 이미 가진 물건을 사라고 했다
AD8232로 이미 배선해서 돌리던 사람에게 브레드보드·점퍼선을 사라고 목록에 넣었다.
**부품을 추천하기 전에 지금 뭘 갖고 있는지 확인해라.** (아래 1절)

### ⑤ 실측하지 않은 값을 상수로 박았다
AD8232 레일을 전원 전압에서 짐작해 3.0 V / 0.3 V 로 코딩했는데, **이 보드의 실측값은
2.57–2.60 V / −0.59 ~ −0.61 V** 였다. 그래서 2.595 V 포화가 정상으로 통과했다.
지금은 값 판정에 더해 "출력이 안 움직이는지"(`STUCK_STD_V`)를 같이 본다.

### ⑥ `os.path.samefile` 은 심볼릭 링크를 따라간다
venv 의 `bin/python3` 는 원본 인터프리터를 가리키는 심볼릭 링크라 `samefile` 이 항상
참이었고, venv 자동 전환이 **한 번도 동작한 적이 없었다**(`5f4d869`). 지금은
`sys.prefix` 와 venv 루트 경로를 문자열로 비교한다.

### ⑦ 시뮬레이터에 공통 표류가 없었다
두 채널의 전극 표류를 서로 독립인 random walk 로만 만들어서, 참조 채널을 빼도
아무것도 안 걷어졌다. 실제 전극 표류는 온도·습도 때문에 **상당 부분이 공통**이다.
`_common_drift(t)` 를 추가했다 — t 만의 함수라 두 채널이 정확히 같은 값을 받는다.
독립 성분은 빼도 안 사라지고 오히려 √2 배로 커지므로, **참조 차감이 이득인지는
공통 성분이 독립 성분보다 큰가에 달려 있다.** 실제 하드웨어에서 확인해야 한다.

### ⑧ 잡음의 원인을 접촉으로 짐작했다 — 실제로는 선의 움직임이었다
"손으로 줄을 잡으면 튄다"는 말을 듣고 인체 전위 결합이라고 답했다. 사용자가
직접 확인한 결과는 달랐다.

| 해본 것 | 결과 |
|---|---|
| 손으로 줄 잡기 | 반응 없음 |
| 식물 어루만지기 | 반응 없음 |
| 선 흔들기 | **요동침** |
| **아무것도 연결 안 하고** 선 흔들기 | **요동침** |

식물이 없어도 튄다. 원인은 접촉이 아니라 **배선 진동**이다. 기전은 둘이다 —
선이 휘며 생기는 마찰전기(1 GΩ에서 1 pA = 1 mV)와, 악어클립이 밀리며 바뀌는
전극 접촉 전위.

**이건 DC 앞단으로 바꿔도 해결되지 않는다.** 오히려 나빠진다. 입력 임피던스가
더 높아 마찰전기에 예민하고, 고역통과가 없어 접촉 튐이 그대로 들어온다.
해결은 기구적이다 — 전극 바로 옆을 고정, 선을 짧게·꼬아서, 측정 중 무접촉.

**이 프로젝트의 첫 실측 관찰이다.** 부품 탓, 회로 탓을 하기 전에 선부터 고정.

### ⑨ 기타 실수
- `session_holdout_split(X, y, groups, labels)` 에 `groups` 를 `labels` 로 넘겨
  "모든 상태의 세션이 1개"라는 헛경고가 났다. `labels` 자리에는 **`y`** 를 넘긴다.
- `MIN_STD_V` 를 0.0005 V 고정으로 두어 DC 앞단 데이터 896창이 전멸했다. 지금은
  입력 환산값(`MIN_STD_INPUT_UV`)에 앞단 이득을 곱해 구한다.
- **한국 상용전원은 60 Hz다.** 시뮬레이터가 50 Hz로 되어 있어 가짜 데이터를 구별하는
  단서가 됐다. 노치 기본값·시뮬레이터 모두 60 Hz 여야 한다.

---

## 1. 하드웨어 — 갖고 있는 것 / 주문한 것

**이미 있음 (다시 사라고 하지 말 것)**
- Raspberry Pi 5, ADS1115(16bit ADC, I2C1 핀 3·5), AD8232 모듈
- 브레드보드, 점퍼선, 악어클립, 구리선, 전도성 젤
- 방울토마토 화분

**주문함 (2026-08-27 도착)**
- MCP6002-I/P (DIP-8, 5개) — DC 결합 앞단용 듀얼 오피앰프
- 세라믹 커패시터 104 (0.1 µF) 100개
- 저항 (100 kΩ 포함)

**안 사도 되는 것**: 10 µF 전해 — 바이어스 노드 잡음은 ADS1115 차동 입력이
공통 성분으로 빼주므로 0.1 µF 로 충분하다.

### DC 결합 회로 (부품 오면 만들 것)
```
1 OUT_A ─┬─→ ADS1115 A0        전압 폴로어 A (이득 1배)
2 IN_A- ─┘
3 IN_A+ ←── 전극 1 (줄기 위)
4 VSS   ←── GND
5 IN_B+ ←── 전극 2 (줄기 아래)
6 IN_B- ─┐                     전압 폴로어 B
7 OUT_B ─┴─→ ADS1115 A1
8 VDD   ←── 3.3 V  (+ 0.1µF 를 8–4 사이)

바이어스: 3.3V ─[100k]─┬─[100k]─ GND
                       ├─ 0.1µF ─ GND
                       └──────────→ 흙 전극 (기준)

참조(더미) 채널 — 두 번째 MCP6002 를 써서 A2-A3 에 같은 회로를 하나 더.
전극쌍을 식물이 아니라 죽은 줄기나 젤 덩어리에 담근다.
```
PGA 16 → ±256 mV, 분해능 7.81 µV. 다만 진짜 한계는 ADC가 아니라
**1 GΩ 전극의 열잡음 17.9 µV rms** 와 **전극 드리프트**다.

---

## 2. 코드 구조

```
main.py                진입점. --web / --no-gui / --sim_csv
src/
  venv_boot.py         venv 자동 전환. 다른 import 보다 먼저 ensure() 호출
  frontend.py          아날로그 앞단 추상화 — AD8232Frontend / DCFrontend
  sensor_control.py    수집. collect() 가 CSV + .meta.json 을 쓴다
                       --ref 를 주면 voltage_ref 열을 같이 기록
  preprocess.py        대역통과·노치 → 10초 창(2초 hop) → 스펙트로그램 + 특징
  feature_extraction.py  17개 특징 (시간 7 + 주파수 7 + slow 3)
  train.py             SVM / RandomForest, 세션 단위 분할·교차검증
  inference.py         RealtimeClassifier. preprocess 와 같은 필터 체인 공유
  web_dashboard.py     Flask. 수집·전처리·학습·판정을 브라우저에서
  sanity_check.py      분할 누수 검사 + 우연 수준 검사
report/                tpl.html 하나만 고치면 나머지는 파생
```

### 반드시 지킬 것
- **앞단 사양을 코드에 흩뿌리지 마라.** 이득·대역·레일은 `frontend.py` 가 값으로
  들고 있고 나머지는 물어본다. `GML_FRONTEND=dc` 또는 `--frontend dc`.
- **학습과 추론은 같은 함수를 쓴다.** `preprocess.bandpass_filter` 와
  `inference.bandpass_filter` 의 필터 체인이 어긋나면 안 된다.
- **분할은 세션 단위로.** 창이 80% 겹치므로 무작위 분할은 정확도를 부풀린다.
  경계 폐기 개수는 `overlap_guard()` 가 `⌈W/S⌉−1` 로 계산한다 — 상수로 박지 마라.
- 상태마다 세션이 하나면 **전극 드리프트 방향이 곧 클래스 라벨**이 된다.
  날짜·전극을 달리해 2회 이상 나눠 수집해야 한다.

### 참조(더미) 채널
```bash
python3 src/sensor_control.py --state 수분부족 --frontend dc --ref --rate 5 --duration 0
```
식물 없이 전극쌍만 담근 채널(A2-A3)을 동시에 기록해 `voltage_ref` 열에 넣는다.
`preprocess.py` 가 기본으로 빼며(`--no-ref-subtract` 로 끔), 거기 남는 것이
식물에서 온 성분이다. 시뮬레이션에서 진짜 신호와의 오차가 **1.706 → 0.062 mV**
로 줄었다. 느린 신호(수분부족)는 이것 없이는 전극 표류와 구분이 안 된다.

⚠️ ADS1115 는 차동쌍이 둘뿐이라 A2-A3 가 마지막이고, 번갈아 읽느라 **채널당
샘플레이트가 절반**이 된다. 자극처럼 빠른 신호를 볼 때는 `--ref` 를 끄는 게 낫다.

---

## 3. 보고서 (`report/`)

```bash
cd report
python3 parse2.py                 # tpl.html → blocks.json
python3 build_docx.py --compact   # 20쪽 판
python3 pagecount.py GML_결과보고서_20쪽판.docx
python3 make_figs.py              # 그림 SVG → PNG (300dpi 이상)
python3 renumber.py               # 표·그림 번호 검사 (--write 로 적용)
```

그림을 추가할 때는 **세 곳**을 같이 고쳐야 한다 — `make_figs.py` 의 `FIGS`,
`build_docx.py` 의 `PLATES`(본문 등장 순서대로!), 그리고 `tpl.html` 의 `{{FIG_*}}`.
하나라도 빠지면 `IndexError` 가 나거나 엉뚱한 그림이 들어간다.

분량이 넘칠 때는 본문을 깎기 전에 **그림의 가로세로비**를 먼저 본다. 본문 폭이
고정이라 같은 내용도 그림을 넓게 그리면 세로가 줄어든다(그림9를 760→940 으로
넓혀 170pt→137pt). 회로도에서 **선은 곧 연결**이므로 묶음 표시 같은 장식에
선을 쓰면 안 된다 — 글자로 쓴다.

양식이 못박은 값 — **바꾸면 안 된다**:
- 전체 **20페이지 이내**
- 본문 휴먼명조 **11pt** (분량 줄이려고 낮추지 말 것)
- 장 14pt / 절 12pt / 항·목 11pt, 줄간격 160%
- 표 제목은 표 **위** 가운데, 그림 제목은 그림 **아래** 가운데
- 그림은 300 dpi 이상, 라벨은 영어(논문 스타일로 통일함)

`pagecount.py` 는 `document.xml` 높이 합산 추정이라 **±1.5쪽 오차**가 있다.
최종 확인은 한글에서 직접 열어서 해야 한다.

---

## 4. 브랜치

| 브랜치 | 용도 |
|---|---|
| `main` | AD8232 전용 |
| `claude/gml-plant` | 웹 대시보드 작업 |
| `claude/dc-frontend` | **현재 작업 브랜치.** DC 결합 앞단 + 보고서 |

원격 저장소는 `packsunjin/GML_PLANT` 다. 보고서 참고문헌 [7]과 README 의 clone
명령은 이 주소로 되어 있다 — 제출물에 나가는 값이니 건드리지 마라.

⚠️ 클라우드 세션에서는 `git remote` 를 새 이름으로 바꾸지 마라. 세션의 git
프록시가 **세션 시작 때 붙은 이름**으로만 인증을 넣어 주므로, 새 이름으로
바꾸면 푸시가 403 으로 막힌다(`add_repo` 도 소유자가 달라 거절된다). 옛 URL 로
두면 GitHub 이 리다이렉트해 정상 동작한다. 깔끔하게 하려면 새 세션을
GML_PLANT 로 시작하는 수밖에 없다.

로컬 폴더 이름은 저장소와 무관하다. 사용자는 파이에서 `GML_PLANT` 로 바꿔 쓴다.
폴더를 옮겨도 `venv_boot` 는 그대로 동작한다(확인함). 다만 **venv 의 `pip` 는
shebang 에 옛 절대경로가 박혀 있어 깨진다** — `venv/bin/python3 -m pip` 로 쓰거나
venv 를 다시 만들어야 한다.

---

## 5. 웹이 기본이다

사용자는 **브라우저 대시보드로만** 작업한다. CLI 명령을 답으로 주지 마라.

```bash
python3 main.py --web        # 이것만 터미널에서. 나머지는 전부 브라우저
```

관리자 탭: `1 · 수집` → `2 · 변환` → `3 · 학습`, 그리고 `대조군` / `자료` / `센서`.
맨 위 **측정 대상**(식물 / 호일 / 사체)이 세 탭 전부에 적용된다.

CLI(`sensor_control.py` 등)는 디버깅용이다. 사용자에게 안내할 때는
"어느 탭에서 무슨 버튼"으로 말한다.

## 6. 사용자에 대해

고등학생이고 마감이 있다. 다음을 지켜라.

- **한국어로 답한다.** 코드 주석도 한국어다.
- **짧게.** 물어본 것에 답하고 끝낸다. 안 물어본 배경 설명을 붙이지 마라.
- **결정해서 말한다.** "A일 수도 있고 B일 수도 있고"가 아니라 "A로 해"라고 한다.
- **모르면 코드를 읽고 말한다.** 추측으로 답하지 마라. 특히 하드웨어 사양,
  사용자가 가진 물건, 이미 측정했는지 여부.
- **틀렸으면 한 줄로 인정하고 고친다.** 길게 사과하지 마라.

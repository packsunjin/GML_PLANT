# 보고서에 넣을 그림·표 채우기 안내

본문에는 자리표시만 남겨 두고(20쪽 제한), 무엇을 어떻게 넣을지는 여기에 정리했습니다.
각 항목을 채운 뒤 보고서의 해당 자리표시를 실제 그림·표로 바꾸세요.

## 그림2  (직접 촬영)

■ 직접 촬영하여 삽입 · 그림2 실험 장치 전체 사진 본 위치(Ⅲ장 1절, 그림1 아래)에 삽입. 방울토마토 화분 + 브레드보드(AD8232·ADS1115) + 라즈베리파이 5가 한 프레임에 들어오게 전극 3개의 연결 경로가 보이도록 약간 위에서 촬영 그림2. 실험 장치 구성

---

## 그림3  (직접 촬영)

■ 직접 촬영하여 삽입 · 그림3 전극 부착 상태 근접 사진 줄기에 부착된 RA·LA 패드를 접사 로 — 밀착 정도가 보이게 RL 전극이 토양에 삽입된 부분도 함께 그림3. 전극 부착 상태

---

## 그림5–7  (데이터 수집 후)

■ 데이터 수집 후 삽입 · 그림5–7 신호 획득 결과 — 재측정 필요 그림5 정상·자극 상태의 전위신호 파형 — 상태별 CSV 그림6 접촉 이벤트 구간의 스펙트로그램 — 자료 탭의 실제 PNG 그림7 10초 창으로부터의 특징 추출 과정 함께 기록할 값: 실효 샘플레이트, 기준점 전압, 레일 포화 창 비율, 측정 길이 3절의 잡음원(인체 전위 결합·배선 진동)을 배제한 조건에서 재측정한 데이터라야 한다.

---

## 표8  (학습 후)

■ 학습 후 삽입 · 표8 과제·특징 방식별 분류 성능 관리자 패널 학습 탭의 결과 또는 train.py 출력에서 옮긴다. 행: 과제( 3 종 / 정상-수분부족 / 정상-자극) × 방식(픽셀 / 특징) 열: 최적 모델(SVM·RandomForest) · 학습/평가 표본 수 · 정확도 · Precision · Recall 목표치 70 % 달성 여부를 함께 표시 정확도가 높게 나오더라도 3절의 출처 검증을 통과한 데이터인지 반드시 확인한다. 검증 없이 얻은 높은 정확도는 측정 조건의 차이를 학습한 것일 수 있다(2절 (4)).

---

## 그림8  (학습 후)

■ 학습 후 삽입 · 그림8 혼동행렬 models/confusion_matrix_*.png 중 최적 모델의 것을 삽입한다. 어느 상태끼리 헷갈리는지가 보이도록 3 종 모델의 것을 우선 사용 축 라벨이 한글로 나오는지 확인(서식상 그림 내 표기는 한글이 원칙) 그림8. 최적 모델의 혼동행렬

---

## 그림10  (측정 후)

■ 측정 후 삽입 · 그림10 잡음 바닥 실측 — 문헌 진폭과의 비교 문헌 진폭( 21.2 mV )과 본 연구의 잡음을 같은 축에서 비교하려면 본 측정계의 잡음 바닥을 실제로 재야 한다. 전극을 부착한 채 자극 없이 정지 상태로 수집한 CSV에서 대역통과·노치 필터 적용 후의 표준편차를 구한다. 이 값이 21.2 mV보다 충분히 작으면 “신호는 컸으나 입력단 도달 전에 소실됐다”는 해석이 성립하고, 그렇지 않으면 잡음 자체가 원인이 된다.

---

## 그림12  (화면 캡처)

■ 화면 캡처하여 삽입 · 그림12 (선택) 실시간 분류 화면 본 위치에 삽입. 사진이 아니라 화면 캡처 로 충분하다. 대시보드에서 파형·확신도·표준편차 수치가 보이는 화면 브라우저 주소창은 잘라내고 대시보드 영역만 캡처 그림12. 실시간 분류 대시보드

---

